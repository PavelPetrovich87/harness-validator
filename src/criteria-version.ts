/**
 * Criteria versioning for the harness validator.
 * Bumped when validation criteria change (new phases, new rules, removed rules).
 * Separate from the tool version in package.json.
 */
export const CRITERIA_VERSION = '0.1.0';
